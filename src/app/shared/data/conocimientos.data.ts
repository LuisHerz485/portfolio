import { IDataSkill } from '../interfaces';
import { skill } from './skills.data';

export const conocimientos: IDataSkill[] | any = [
    skill.frameworks?.react,
    skill.frameworks?.express,
    skill.tools?.tailwind,
    skill.uiux?.photoshop,
];
